import { createFileRoute } from "@tanstack/react-router"
import { Markdown } from "../components/Markdown.js"
import src from "../../docs-content/ai.md?raw"

export const Route = createFileRoute("/docs/ai")({
  component: Page
})

// Single source: this markdown also ships as /docs/ai.md for agents
// (and inside llms-full.txt), generated by scripts/gen-llms.ts.
function Page() {
  return (
    <>
      <span className="spec-tag">AI Copilot</span>
      <Markdown src={src} />
    </>
  )
}
