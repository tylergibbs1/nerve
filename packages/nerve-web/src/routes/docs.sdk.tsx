import { createFileRoute } from "@tanstack/react-router"
import { Markdown } from "../components/Markdown.js"
import src from "../../docs-content/sdk.md?raw"

export const Route = createFileRoute("/docs/sdk")({
  component: Page
})

// Single source: this markdown also ships as /docs/sdk.md for agents
// (and inside llms-full.txt), generated by scripts/gen-llms.ts.
function Page() {
  return (
    <>
      <span className="spec-tag">TypeScript SDK</span>
      <Markdown src={src} />
    </>
  )
}
