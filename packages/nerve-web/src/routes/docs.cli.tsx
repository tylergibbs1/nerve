import { createFileRoute } from "@tanstack/react-router"
import { Markdown } from "../components/Markdown.js"
import src from "../../docs-content/cli.md?raw"

export const Route = createFileRoute("/docs/cli")({
  component: Page
})

// Single source: this markdown also ships as /docs/cli.md for agents
// (and inside llms-full.txt), generated by scripts/gen-llms.ts.
function Page() {
  return (
    <>
      <span className="spec-tag">CLI</span>
      <Markdown src={src} />
    </>
  )
}
