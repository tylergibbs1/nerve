import { createFileRoute } from "@tanstack/react-router"
import { Markdown } from "../components/Markdown.js"
import { docsContent } from "../docs/content.js"

export const Route = createFileRoute("/docs/dsl")({
  head: () => ({ meta: [{ title: "DSL reference · Grayhaven Nerve" }] }),
  component: Page
})

// Single source: this markdown also ships as /docs/dsl.md for agents
// (and inside llms-full.txt), generated by scripts/gen-llms.ts.
function Page() {
  return (
    <>
      <span className="spec-tag">DSL Reference</span>
      <Markdown src={docsContent("dsl")} />
    </>
  )
}
